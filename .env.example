# Analytics only, and entirely optional — track() no-ops when the key is unset.
# There is no backend: no database, no auth, no webhooks. If you are looking for
# the Instagram API and Supabase variables that used to live here, the stack
# that needed them was deleted in f58d1c5.
NEXT_PUBLIC_POSTHOG_KEY=
NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com
