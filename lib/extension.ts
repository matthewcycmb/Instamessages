/**
 * Chrome Web Store listing for the blocker extension. Null until the listing
 * is approved — the install buttons then fall back to the load-unpacked
 * steps with the zip served from /extension.zip.
 */
export const CHROME_STORE_URL: string | null = null;
